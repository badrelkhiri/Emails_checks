// let validAccounts = [];

// chrome.runtime.onMessage.addListener((request, sender, sendResponse) => {
//   if (request.action === "saveValidAccount") {
//     validAccounts.push(request.account);
//     console.log("Saved valid account:", request.account);
//   } else if (request.action === "getValidAccounts") {
//     sendResponse({ accounts: validAccounts });
//   }
// });
